export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type TalentType = {
  id: string;
  name: string;
  email: string;
  image_url: string;
  bio: string;
  talent: string;
};
